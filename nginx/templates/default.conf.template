# Prod gateway vhosts are generated — do not add server blocks here.
#
# Source:  nginx/tenants.prod.json
# Generate: ./scripts/generate-nginx-tenant-servers.sh
# Output:   nginx/generated/tenant-servers.conf.template (committed; mounted in prod)
#
# The gateway image and docker-compose mount the generated template as
# /etc/nginx/templates/default.conf.template.
